const { max } = require("underscore");
const _=require("underscore");
// check either it is core module or not
  // then check  it is file or directory
      // then check inside node_modules directory

console.log(_.contains([23,44,2,54],2));     
console.log(max([5,6,8,23,54])); 