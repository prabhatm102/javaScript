const logger=require("./logger.js");

//global.console.log(global);
var a="Hello";
// console.log(global.a); //global i same as window but variables are not combine with global object.

//console.log(logger);
logger.log("hello!!!");
console.log(logger.url);
